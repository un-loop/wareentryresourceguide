{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against dev",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceRoot}/out/dev"
        },
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against prod",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceRoot}/out/prod"
        }
    ]
}